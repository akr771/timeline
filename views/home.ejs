<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h>hallo akram</h>
    <div class="users">
        <%for (let i = 0; i < sortedUsers.length; i++) { %>
                <h2><%= sortedUsers[i].name %>, <%= sortedUsers[i].createdAt %></h2>
            <p><%= sortedUsers[i].message %></p>
        <% } %>
    </div>
</body>
</html>